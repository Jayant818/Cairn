.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
mutants.out*/
fork-ledger/
fork/*-ata.json
fork/atas.json
fork/generated/

# frontend
app/node_modules
app/dist
# ⛔ BEFORE the first frontend commit, not after: a secret committed once is committed forever,
# and the rotation that follows is someone else's afternoon.
app/.env
app/.env.*
!app/.env.example
